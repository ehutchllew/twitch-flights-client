import React from 'react';
import { TitleBar } from '../common/titleBar/TitleBar';

export const HomeScreen = () => {
    return (
        <div>
            <TitleBar />
            <h2>Home Page</h2>
        </div>
    )
}